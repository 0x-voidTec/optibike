<?xml version="1.0" encoding="utf-8"?>
<!-- OptiBike - Bike Fitting App -->
<!-- AndroidManifest.xml -->
<!-- Generated by Vibe Code (AI Agent) -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- App metadata -->
    <application
        android:name=".OptiBikeApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/optibike_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/optibike_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.OptiBike"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.OptiBike">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ARCore Activity (for AR measurements) -->
        <activity
            android:name=".ar.ARMeasurementActivity"
            android:exported="false"
            android:theme="@style/Theme.OptiBike" />

        <!-- File provider for PDF export -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->
    <!-- Camera for AR measurements -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Storage for PDF export -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <!-- ARCore requires these features -->
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />

    <!-- Screen orientation -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:requiresSmallestWidthDp="360" />

</manifest>
